import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.myschooldesk.myattendancedesk',
  appName: 'My AttendanceDesk',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
